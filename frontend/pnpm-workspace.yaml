allowBuilds:
  esbuild: true
  vue-demi: true

# pnpm 10+ no longer reads the "pnpm" field from package.json, so these
# security pins live here. Keep them in sync with any advisory fixes.
overrides:
  js-cookie: 3.0.7
  form-data@<4.0.6: '>=4.0.6'
  postcss@<8.5.18: '>=8.5.18'
